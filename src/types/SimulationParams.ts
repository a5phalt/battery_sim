// ============================================
// ТИПЫ ДАННЫХ: ПАРАМЕТРЫ СИМУЛЯЦИИ
// ============================================

import type { SimulationMode } from './BatteryType';

// Что пользователь задаёт перед запуском симуляции.
// Это НЕ пресет — пресет про "железо", а это про "условия опыта".
export interface SimulationParams {
  presetId: string;             // ID выбранного пресета
  mode: SimulationMode;         // Режим: 'charge' или 'discharge'
  initialSoc: number;           // Начальный уровень заряда (%)
                                // Для разряда обычно 100, для заряда — 0
  timeStep: number;             // Шаг расчёта (с) — технический парамет
}